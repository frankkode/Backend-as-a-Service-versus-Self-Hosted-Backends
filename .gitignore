django-variant/django-variant/
django-variant/.env
.env.keys
.env.local
.env.*.local
.env
.venv/

# Python bytecode / caches
__pycache__/
*.pyc
.coverage
.pytest_cache/

# Supabase CLI local state (contains live project ref, org id, pooler host)
.temp/
**/.temp/
.branches/

# OS junk
.DS_Store

# ops-effort measurement scratch state
.ops_timer/

# read the wrong file.
*.bak
*.pre_run3_bak

# Machine-specific operational runbook: contains the live host address and root SSH steps for one
# server. 
DEPLOY_srv*.md

# Private working material
Defense_Arguments*
defence*.md
*self-report*
*self_report*
*worksheet*
*supervisor*
screenshots/
*_private.*
*_PRIVATE.*
